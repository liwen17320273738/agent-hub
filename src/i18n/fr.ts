// French — until `pnpm i18n:sync -- --locales fr` overwrites this file, reuse English
// so the locale is complete (no empty-bundle + fallback mix). Translations = copy of EN for now.
import en from './en'
export default en
