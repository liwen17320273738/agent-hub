# 2026-05-07

## 完成：UI设计稿 + 架构图可视化

### 1. 扩展 UiVisualizer 支持架构图生成
- 新增 `generate_architecture_diagram()` — 从架构 spec 解析系统组件和交互流
- 新增 `_parse_architecture_spec()` — 智能识别 frontend/backend/db/cache/auth/ai 等组件
- 新增 `_generate_mermaid_diagrams()` — 生成三种 Mermaid 图：系统架构总览(flowchart)、核心交互流程(sequenceDiagram)、分层部署视图(flowchart LR)
- 新增 `_generate_arch_html()` — 包装 Mermaid.js CDN 渲染的独立 HTML 页面，带 dark 主题和组件清单
- 文件：`backend/app/services/ui_visualizer.py`

### 2. 注册架构图 artifact 类型
- `architecture_diagram` 注册到 `BUILTIN_ARTIFACT_TYPES`，category=diagram
- 更新 `_write_one_artifact` 支持 `metadata_json` 参数（存储 filePath、componentCount 等）
- 文件：`backend/app/models/task_artifact.py`, `backend/app/services/artifact_writer.py`

### 3. Pipeline 集成改造
- `design` 阶段 → UI mockup（保留原有行为）
- `architecture` 阶段 → 架构图（新行为）
- 使用 `_write_one_artifact` 直接写入自定义 artifact 类型
- 文件：`backend/app/services/pipeline_engine.py`

### 4. 前端组件
- 创建 `TaskArchDiagram.vue` — 渲染 Mermaid 架构图 iframe，支持 HTML 路径和源码降级
- 更新 `TaskArtifactTabs.vue` — TAB_DEFS 新增 `ui_mockup`/`ui_mockup_html`/`architecture_diagram`
- 更新 `TaskDocTab.vue` — 添加新类型的 doc 映射

### 5. 启动修复（非本次功能引入的预存问题）
- `tools/registry.py`：`image_gen_tool` 模块缺失 → 改用 try/except 条件导入+条件注册
- `agents/seed.py`：agent 数据含 `version`/`is_builtin`/`tags`/`rules`/`hooks`/`mcp_tools`/`execution_mode` 等非模型字段 → pop 清理后传入
- `agents/seed.py`：`ui-visual-designer` 缺少 `title` 字段 → 补充

### 启动验证
- 后端 8000 → 200 ✅（LLM 探针通过 local + zhipu 提供者）
- 前端 5200 → 200 ✅

### 6. E2E 验证
- 直接调用 `UiVisualizer.generate_architecture_diagram()` 生成架构图 ✅
  - 11 组件识别、10 条数据流
  - 3 种 Mermaid 图（architecture/sequence/deployment）
  - HTML 输出通过 iframe 渲染
- DB 验证：`architecture_diagram`、`ui_mockup`、`ui_mockup_html` 类型已注册 ✅
- API 验证：`GET /tasks/{id}/artifacts` 返回 15 种类型（含 3 种新类型）✅
- Pipeline E2E：gemma4-26b 推理模型太慢，planning 阶段跑了 8 分钟。全流程 12 阶段预估 60-120 分钟，暂不等待。
