# 2026-04-30 工作日志

## AgentRuntime 数据库事务问题修复

- 根因：`run_agent_stream` 的 `_runner()` 后台 task 与 FastAPI `get_db()` 的上下文管理器竞争同一个 db session
- 修复：`_runner()` 内部创建独立的 `async_session()`，不再共享 endpoint 的 db session
- 验证：stream API 返回正常 SSE 流（started → progress → completed），不再出现 "prepared state" 错误
- 提交：ffc044c

## Issue 21 artifact 系统修复

- worktree.py：
  - 删除重复的 `_find_task_root`，统一使用 `task_workspace.find_task_root`
  - 扩展 `_TEXT_EXTENSIONS`（+.ini, .cfg, .lock, .dockerignore, .editorconfig, .conf, .log 等）
  - 新增 `_is_code_file()` + `_CODE_EXTENSIONS`，统计所有代码文件（不限于 src/）
  - 文件树列表跳过隐藏文件/目录和 `_archive`
- workspace_archiver.py：
  - 归档时包含根目录的松散文件（app.py, Dockerfile 等），不只是子目录
  - 跳过 `_archive` 目录避免递归归档
- TaskArtifactTabs.vue：code_link icon 与后端对齐（'💻' -> '📦'）
- 验证：Python 语法通过、TypeScript 编译通过、11 个测试通过
- 提交：ffc044c（与 AgentRuntime 修复同一次提交）

## 今日提交汇总

| Commit | 内容 |
|--------|------|
| 689874e | Issue 24 Phase 3：CodeGenAgent 接入 + worktree 质量评分 |
| ffc044c | Issue 21 artifact 修复 + AgentRuntime stream DB session 修复 |
