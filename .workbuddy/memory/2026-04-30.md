# 2026-04-30 工作日志

- 分析了 liwen17320273738 GitHub 账号下 22 个仓库（3 原创 + 19 fork），输出完整分析报告到 artifact
- 筛选出 7 个高优先级可集成项目：agency-agents-zh(角色扩展)、oh-my-claudecode(多模型并行)、everything-claude-code(技能库)、gstack(Sprint方法论)、superpowers(SKILL格式)、spec-kit(规范驱动)、firecrawl(网页采集)
- 发现 agent-hub 与这些项目的最大差距在技能生态（当前 16 vs 项目的 181 个技能）
- Sprint 0-5 全部完成：15个SKILL.md添加trigger_stages、KnowledgeCollection/StageRunLog模型、spec_driven DAG模板、GitHub Webhook、hub-cli CLI工具等
- 数据库迁移到阿里云服务器 47.95.254.24:5433：启动系统 PostgreSQL 16、改端口至5433、创建agenthub用户和库、补workspaces/workspace_members迁移文件、stamp至head版本c4d5e6f7a8b9共40张表
- .env 更新 DATABASE_URL 指向远程服务器，本地asyncpg直连验证通过
- agent-hub 独特优势：中国本土化（微信/企微集成）

## AgentRuntime 数据库事务问题修复

- 根因：`run_agent_stream` 的 `_runner()` 后台 task 与 FastAPI `get_db()` 的上下文管理器竞争同一个 db session
- 修复：`_runner()` 内部创建独立的 `async_session()`，不再共享 endpoint 的 db session
- 验证：stream API 返回正常 SSE 流（started → progress → completed），不再出现 "prepared state" 错误
- 提交：ffc044c

## Issue 21 artifact 系统修复

- worktree.py：
  - 删除重复的 `_find_task_root`，统一使用 `task_workspace.find_task_root`
  - 扩展 `_TEXT_EXTENSIONS`（+.ini, .cfg, .lock, .dockerignore, .editorconfig, .conf, .log 等）
  - 新增 `_is_code_file()` + `_CODE_EXTENSIONS`，统计所有代码文件（不限于 src/）
  - 文件树列表跳过隐藏文件/目录和 `_archive`
- workspace_archiver.py：
  - 归档时包含根目录的松散文件（app.py, Dockerfile 等），不只是子目录
  - 跳过 `_archive` 目录避免递归归档
- TaskArtifactTabs.vue：code_link icon 与后端对齐（'💻' -> '📦'）
- 验证：Python 语法通过、TypeScript 编译通过、11 个测试通过
- 提交：ffc044c（与 AgentRuntime 修复同一次提交）

## 今日提交汇总

| Commit | 内容 |
|--------|------|
| 689874e | Issue 24 Phase 3：CodeGenAgent 接入 + worktree 质量评分 |
| ffc044c | Issue 21 artifact 修复 + AgentRuntime stream DB session 修复 |

## Fork 项目分析 + 补短执行

对比 22 个 fork 项目输出 3 份产物：fork-repos-analysis.md / agent-hub-gap-analysis.md / agent-hub-execution-plan.md
用户深度校正后明确 5 条补短优先级。实际执行：

### SKILL.md trigger_stages
- 15 个 public SKILL.md 全部加了 `trigger_stages`（精确匹配 pipeline 阶段）
- 修复 `sync_skills_to_db()` 缺失的 trigger_stages/completion_criteria/allowed_tools/execution_mode 同步

### KnowledgeCollection + Ingester
- 新增 KnowledgeCollection ORM 模型 + collection_id FK 到 TaskMemory
- 创建 migration (PG/SQLite 双兼容)
- search_similar_memories 增加 collection_id 过滤（pgvector + keyword）
- KnowledgeCollection CRUD API + POST /memory/ingest-url
- services/ingester.py：firecrawl scrape → chunk → TaskMemory
- 新增 firecrawl MCP 预设到 mcpPresets.ts

### Sprint 3: Git 原生引用 + Webhook
- PipelineTask 加 `repo_refs` JSONB 字段（PR/分支/CI 状态）
- migration `b3c4d5e6f7a8_add_pipeline_task_repo_refs.py`
- GitHub Webhook 端点 `POST /api/gateway/webhooks/github`（PR/check_run/push）
- UpdateTaskRequest 增加 repo_refs/repo_url/project_path
- PipelineTaskDetail.vue 增加 Git 状态行显示

### Sprint 4: IDE/CLI 同步
- `GET /api/pipeline/tasks/{id}/context-bundle` — 导出任务上下文 JSON（含 CLAUDE.md 文本）
- `scripts/hub-cli.sh` — CLI 同步工具（context/status/sync/list）

### Sprint 5: 画布可观测性
- StageRunLog ORM 模型（每次执行一条记录）
- migration `c4d5e6f7a8b9_add_stage_run_logs_table.py`
- DAG orchestrator 自动记录每次 node 执行（input/output/quality/error/duration）
- `GET /api/pipeline/tasks/{id}/stage-run-logs` — 查询执行历史
- `POST /api/pipeline/tasks/{id}/retry-stage/{stage_id}` — 重试失败节点
