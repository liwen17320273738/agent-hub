# 2026-05-06 工作日志

- 修复三个短板：Claude Code 前端日志面板、Agent 协作仪表盘、Hermes 监督模块
- **Claude Code 日志面板**：后端新增 GET /executor/jobs/{job_id}/logs 端点；前端创建 ExecutionLogTab.vue 组件（多 Job 切换、日志过滤/搜索、实时 SSE 流），集成到 PipelineTaskDetail.vue 的第5个 Tab
- **Agent 协作仪表盘**：重写 Team.vue 为协作仪表盘，增加实时 Agent 状态指示灯、流水线状态条、协作关系图（展示评审链和升级路径）
- **Ruflo + VibeVoice 集成评估**：完成两个开源项目的全面分析，输出 docs/ruflo-vibevoice-集成评估报告.md
  - Ruflo (43.7k⭐, MIT): 15-Agent 蜂群编排 + SONA 自学习记忆 + MCP 协议。推荐 P1 接入，通过 HTTP MCP Bridge 以 sidecar 方式集成，0 侵入 Python 核心
  - VibeVoice (Microsoft, MIT): ASR 7B + Realtime 0.5B 语音能力。推荐 P2，ASR 可跑 M5 Pro 本地或 HuggingFace API
- **Ruflo 集成完成**：
  - `mcp_bridge.py` — MCP JSON-RPC 客户端，封装 Agent/Swarm/Memory/Config 四组工具的异步调用
  - `pipeline_engine.py` — 在 execute_stage 的 LLM 调用前插入 ruflo_memory_enrich（跨会话记忆增强），阶段完成后存储输出到 Ruflo
  - `config.py` — 添加 ruflo_enabled / ruflo_cmd / ruflo_memory_enrich / ruflo_auto_swarm 四个配置项
  - `Dockerfile` — 安装 Node.js 22 + ruflo@latest
  - `executor.py` — 新增 GET /executor/ruflo/status 健康检查端点
  - `config.example.yaml` — 添加配说明
  - E2E 验证：MCP 桥接初始化、memory_store/retrieve/search、swarm_status、agent_list 全部通过
- **VibeVoice 集成完成**：
  - `vibevoice_proxy.py` — 语音转文本代理服务，支持 HuggingFace Inference API 和本地 GPU 两种模式，mock 模式可无 API Key 使用
  - `gateway.py` — 新增 POST /gateway/vibevoice/transcribe（音频上传/URL转写 + 自动创建 Pipeline 任务）+ GET /gateway/vibevoice/status
  - `config.py` — 添加 vibevoice_enabled / vibevoice_force_local / hf_api_key 配置项
  - `Dashboard.vue` — Hero 区集成 VoiceInput.vue 语音按钮
  - E2E 验证：mock 模式返回转录结果，真实模式需 HF_API_KEY
- **Hermes 监督模块**：创建 skills/public/hermes-oversight/SKILL.md；创建 backend/app/services/hermes_oversight.py（整合 self_verify、quality_gate、guardrail、peer_review、observability、final_acceptance 6个模块）；注册 hermes-overseer Agent 和技能到 seed.py；pipeline_engine.py 中集成 Hermes 检查；迁移 28b5a2d57b6a 新增 hermes_score/hermes_verdict 列
- 数据库迁移至阿里云 PostgreSQL（47.95.254.24:5433），系统 PostgreSQL 16 已启动，agenthub 用户/库已创建，/executor 端点新增日志获取接口；Hermes 迁移已执行至 head
- **Agent Hub 代码审查修复执行完成**（6/6）：
  - P0-#4 ✅ GitHub webhook 添加 HMAC-SHA256 签名验证 + github_webhook_secret 配置项（未设置时返回 503）
  - P0-#1 ✅ config.py 移除硬编码凭据：admin_password/changeme → 空字符串、DB URL 凭据 → 环境变量 POSTGRES_*、_bootstrap_admin 凭据为空时跳过
  - P0-#2 ✅ docker-compose.yml REDIS_URL 密码回退从 :-changeme → :?Set 强制要求
  - P0-#3 ✅ 新增 gateway_openclaw_secret 配置项，_require_openclaw_secret 优先使用它，fallback 到 pipeline_api_key
  - P1-#5 ✅ main.py 添加 admin_password 生产环境弱口令检查（<12字符或常见弱密码拒绝启动）
  - P1-#6 ✅ gateway.py 13处裸异常吞没全部添加 logger.debug/warning