{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cat >/dev/null || true; git rev-parse --git-dir >/dev/null 2>&1 && code-review-graph update --skip-flows --repo \"/Users/wayne/Documents/agent-hub\" || true",
            "timeout": 30
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$FILE_PATH\" | grep -qE '\\.(vue|ts|js)$'; then cd \"$PROJECT_DIR\" && npx eslint --fix \"$FILE_PATH\" 2>/dev/null || true; fi"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$FILE_PATH\" | grep -qE '\\.py$'; then cd \"$PROJECT_DIR\"/backend && python3 -m ruff check --fix \"$FILE_PATH\" 2>/dev/null && python3 -m ruff format --quiet \"$FILE_PATH\" 2>/dev/null || true; fi"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cat >/dev/null || true; git rev-parse --git-dir >/dev/null 2>&1 && code-review-graph status --repo \"/Users/wayne/Documents/agent-hub\" || echo 'Not a git repo, skipping'",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$PROJECT_DIR\"/backend && python3 -m ruff check . 2>/dev/null && echo '[Hook] Backend lint OK' || echo '[Hook] Backend lint has issues'"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$PROJECT_DIR\" && npx eslint src/ --ext .vue,.ts,.js 2>/dev/null && echo '[Hook] Frontend lint OK' || echo '[Hook] Frontend lint has issues'"
          }
        ]
      }
    ]
  }
}
