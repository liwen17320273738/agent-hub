# 生产构建时设为 true，且部署时配置与 Vite 相同的 /api/proxy/* 反向代理，请求将走同源路径
# VITE_USE_RELATIVE_PROXY=true

# 企业模式（需同时运行 server/index.mjs）；详见 README 与 .env.enterprise.example
# VITE_ENTERPRISE=true
# VITE_API_BASE=/api/hub
